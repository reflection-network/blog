## Title

The Agent Is a Repo

## Description

An agent defined in 17 lines of Nix. A schema that validates identity at eval time. Two repos, zero frameworks.

## Key themes

- 17 lines of Nix: a comically simple agent definition
- Eval-time assertions: schema validation before anything is built
- Pure identity, no machinery: capsule knows nothing about infrastructure
- Schema layering: mkAgent returns flake outputs, capsule is just a shell
- Two repos: agent.nix (schema) + capsule (identity)

## Tweet

An agent defined in 17 lines of Nix. A schema that validates identity at eval time. The capsule knows nothing about infrastructure — pure identity, no machinery.

https://blog.reflection.network/001-agent-is-a-repo/

## Image idea

Wireframe polyhedron with visible nested layers inside — a config schema made solid. Self-contained, minimal. A smaller shape nearby connected by thin lines — schema and capsule as two linked structures.

## Image prompt

Abstract futuristic blueprint-style artwork on a deep navy background (#1a1a2e), drawn with thin technical lines in light gray (#e0e0e0).

A precise wireframe polyhedron at the center — clean edges, visible internal structure with nested layers inside, like a configuration schema made solid. The shape is self-contained, complete, minimal. Around it, faint construction lines show how it was assembled — radial guides, alignment marks. Two thin connection lines extend from the polyhedron to a smaller, simpler shape nearby — the relationship between schema and capsule.

Include faint blueprint grid lines and technical construction marks, but keep them minimal and clean.

No text, no labels, no UI elements — purely abstract.

Style: minimal, precise, engineering blueprint aesthetic, high contrast, lots of negative space, strict geometry.

No gradients, no glow effects beyond very subtle line intensity variation.

Mood: calm, systemic, engineered.
